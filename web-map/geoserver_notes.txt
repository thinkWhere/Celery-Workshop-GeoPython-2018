mkdir C:/dev/geoserver_data
docker run --name=geoserver_8085 -p 8085:8080 -d -v C:/dev/geoserver_datadir:/opt/geoserver/data_dir -e "GEOSERVER_LOG_LOCATION=/opt/geoserver/data_dir/logs/geoserver_8085.log" -t thinkwhere/geoserver

geoserver

make workspace
name: geopython
Namespace: http://thinkwhere/geopython
Go in to workspace and enable wms

Make store:
datasource_name: geopython
host: postgis
port: 5432
database: geopython-db
schema: geopython
user: geopython
passwd: geopython

Add Layer:

grid_square > publish
compute bounding boxes from srs bounds

create style
upload grid_squares.sld

Apply style to grid_squares layer





